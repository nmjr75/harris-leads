# Secrets — never commit these
.env
*.env
supabase/.env
!.env.example
!supabase/.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
venv/
.venv/
env/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Local scratch
scratch/
tmp/

# Legacy JSON dumps — Supabase is the source of truth now.
# Keeping these tracked grew the repo to >100 MB and broke pushes.
# Scraper still writes them locally, but they no longer ship to git.
dashboard/records.json
data/records.json
data/ghl_export_*.csv
